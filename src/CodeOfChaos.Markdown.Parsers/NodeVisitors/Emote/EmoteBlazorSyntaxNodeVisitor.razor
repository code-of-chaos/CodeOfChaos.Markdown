@* ------------------------------------------------------------------------------------------------------------------ *@
@* Imports
@* ------------------------------------------------------------------------------------------------------------------ *@
@using CodeOfChaos.Markdown.Parsers.Blazor.Services
@using CodeOfChaos.Markdown.Syntax.Nodes
@using CodeOfChaos.Markdown.Parsers.Langs.Blazor
@using Microsoft.Extensions.DependencyInjection

@namespace CodeOfChaos.Markdown.Parsers.NodeVisitors
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Descriptors
@* ------------------------------------------------------------------------------------------------------------------ *@
@inherits BaseMarkdownSyntaxNodeVisitor<EmoteMdSyntaxNode>

@inject IServiceProvider ServiceProvider
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Html
@* ------------------------------------------------------------------------------------------------------------------ *@
@if (EmoteDataProvider is not null && EmoteDataProvider.TryGetEmoteUrl(SyntaxNode.EmoteKey, out string? emote)) {
    <img src="@emote" alt="@SyntaxNode.EmoteKey"/>
}
else {
    @SyntaxNode.OriginalEmote
}
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Code
@* ------------------------------------------------------------------------------------------------------------------ *@

@code {
    private IEmoteDataProvider? EmoteDataProvider { get; set; }

    protected override void OnInitialized() {
        base.OnInitialized();
        EmoteDataProvider = ServiceProvider.GetService<IEmoteDataProvider>();
    }
}

